# 前端技术路线审批稿

状态：用户已选择方案 C

日期：2026-09-12

## 已确认的共同约束

- 第一版为中文静态原型，不要求服务器、账号系统或付费服务；
- 数据直接读取现有 JSON 与 Markdown；
- 首页包含自然语言输入、简化全局知识图谱和示范主题入口；
- 图谱页支持点线关系、缩放拖动、节点聚焦、筛选和详情侧栏；
- 用户身份与研究方向只保存在本地浏览器，可跳过，不改变全局数据；
- 输入理解先使用别名、关键词、规则和本地模糊匹配，不作诊断；
- 只有通过审核的医学内容才能在公开视图中标记为已审核。

## 方案 A：轻量原生型

组合：Vite + TypeScript + Cytoscape.js，不使用 React 等界面框架。

原理：由 TypeScript 直接操作网页元素，Cytoscape.js 负责知识图谱绘制和交互，Vite 负责开发预览与静态构建。

优点：

- 依赖少、首屏负担小；
- 没有框架层，原型启动快；
- 可免费构建并部署到静态托管平台。

缺点：

- 搜索结果、筛选、侧栏、阅读模式和多页面状态增多后，手工维护成本较高；
- 组件复用和自动化测试的组织性弱于 React；
- 后续增加更多实体类型时容易出现界面逻辑交织。

适用场景：只做一次性演示、页面数量少、后续功能扩展有限。

## 方案 B：均衡应用型（推荐）

组合：Vite + React + TypeScript + Cytoscape.js。

原理：React 管理搜索框、图谱、详情侧栏、证据卡、筛选器和阅读偏好等界面状态；Cytoscape.js 专门处理图结构、布局和交互；Vite 输出可部署的纯静态文件。

优点：

- 最适合本项目“搜索—图谱—详情—证据”之间频繁联动的界面；
- 可以按页面与组件拆分，六主题内容扩展时更容易维护；
- TypeScript 可尽早发现 JSON 字段和组件输入不一致；
- 不需要后端，不需要付费 AI，也能实现首版全部功能；
- 将来若接入免费或自建 AI，只需替换输入理解层，不必重写图谱和页面。

缺点：

- 相比方案 A 多一层框架和少量构建复杂度；
- 若只做极小型单页演示，会显得稍重；
- 必须控制组件边界，避免为首版过度抽象。

适用场景：希望第一版可展示，同时保留持续增加主题、证据与交互能力的空间。

推荐理由：现有数据已经拆成节点、关系、证据和来源，界面又包含多个相互联动的状态。React 负责界面状态、Cytoscape.js 负责图谱，职责边界清楚，复杂度与可扩展性最均衡。

## 方案 C：内容出版型

组合：Astro + React 交互岛 + TypeScript + Cytoscape.js。

原理：Astro 在构建时生成大部分静态内容页，只在搜索、图谱和阅读偏好区域加载 React；Cytoscape.js 仍负责知识图谱。

优点：

- 疾病、技术和证据详情页可以预生成，首屏和搜索引擎可读性较好；
- 大量纯文字页面不必全部加载 React；
- 适合未来发展为内容型知识门户。

缺点：

- Astro、React 与图谱状态之间需要额外衔接；
- 首版同时维护静态内容层和交互岛，调试链路最长；
- 当前只有少量示范内容，性能收益还不明显。

适用场景：已经确定短期内会扩展到大量可索引详情页，并把内容传播和搜索引擎收录放在最高优先级。

## 暂缓决定的项目

以下选择不随前端主框架一起擅自确定，等页面骨架运行后再按实际需要提交：

- 本地模糊搜索是使用小型库，还是保留纯 TypeScript 实现；
- 图谱首页默认采用力导向、同心圆还是分层布局；
- 是否增加浏览器内小模型作为可选增强；
- 最终免费托管平台。

## 建议审批结论

建议选择方案 B。批准后，第一轮页面实现只建立首页、图谱探索页和统一详情侧栏，接入现有骨架数据，并完成桌面与移动端基础验证；不接入后端或 AI 服务。
