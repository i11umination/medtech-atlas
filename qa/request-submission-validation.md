# 用户需求收集入口验收记录

> 历史快照：其中“待医学审核”标签已由 R1 的“来源已核验”状态取代。

日期：2026-09-12

## 已验证

- 首页六张主题卡已从“只显示关联技术”更新为显示代表性研究发现、研究阶段和证据卡数量；
- 六个已有证据主题的节点状态已由“资料待补充”调整为“待医学审核”，避免把已有候选证据误显示为空白内容；
- 主导航新增“提交需求”，链接到 `/request`；
- `/request` 页面可生成，包含需求类型、主题、具体问题、内容深度和来源线索字段；
- 表单要求提交者确认不填写个人健康信息，并保留隐藏反垃圾字段；
- 页面文案明确：用户需求进入人工策展队列，不会直接变成公开医学结论；
- `functions/api/requests.ts` 对字段类型、长度、枚举值和 D1 未连接状态进行校验；
- `migrations/0001_content_requests.sql` 建立需求队列表、状态约束和状态时间索引；
- `npm run check`：0 errors、0 warnings、0 hints；
- `npm run build`：55 个静态页面构建成功。

## 尚待部署后验证

- 在 Cloudflare D1 创建表并绑定 `DB`；
- 在 Pages 环境中提交一条真实测试需求，确认返回登记编号并在 D1 中出现；
- 配置正式的反垃圾校验（可选 Turnstile）；
- 建立受保护的人工审核入口，审核后再更新 `data/` 和 `evidence/`，由 CI 重新构建发布。

## 安全边界

用户提交内容只进入待处理队列；当前接口不收集姓名、联系方式或病历，也不允许用户直接写入公开知识图谱。
