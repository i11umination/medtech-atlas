# 医学 × 先进技术项目交接

交接日期：2026-09-13

阶段状态：大型内容库第一、二批扩充及图谱结构收束已完成

冻结标签：`stage-2-content-expansion-2026-09-13`

项目定位：具有明确证据边界和审核披露的中文医学技术知识导航站；不提供诊断或治疗建议

## 1. 本阶段结论

本阶段已从少量示范内容推进到可持续扩充的静态知识库：内容模型、入库流程、来源核验规则、覆盖审计、分层导航和前台浏览方式均已形成闭环。

合并后的 16 个批准科学门类已经全部落地。图谱不再以继续增加门类名称为首要目标；下一阶段应转向现有门类的第二条独立临床路径、证据维护以及大型内容库的加载与管理能力。

网站主标语保持为：

> 技术的终极目的，就是解决人类疾苦。

首页说明采用泛化表达，不再把项目范围限定为机械工程、自动化或机器人技术。

## 2. 冻结的数据基线

| 数据对象 | 数量 | 当前状态 |
| --- | ---: | --- |
| 实体节点 | 157 | 143 个 `source-checked`，14 个骨架节点为 `draft` |
| 科学门类 | 16 | 全部门类已落地 |
| 核心能力 | 37 | 作为正式 `CAP-*` 节点维护 |
| 技术 | 32 | 通过核心能力连接科学门类 |
| 疾病与健康状态 | 25 | 前台统一使用该名称 |
| 临床问题 | 22 | 连接疾病、技术与证据路径 |
| 研究节点 | 25 | 用于承载研究与转化语境 |
| 实体关系 | 200 | 128 条 `source-checked`，72 条 `draft` |
| 独立证据卡 | 94 | 全部 `source-checked`、`public` |
| 来源记录 | 122 | 121 条 `source-checked`，1 条撤稿来源为 `blocked` |
| 未来方向 | 4 | 均绑定依据证据并标注为项目分析 |
| 导航分组 | 17 | 覆盖全部实体，无重复主归属 |

疾病层证据集中度已经下降：脑卒中为 22/94（23.4%）；临床问题层最高项为脑卒中后运动功能障碍 20/94（21.3%）。

## 3. 本阶段完成的内容建设

### 3.1 门类与路径扩充

- 完成第一批工程、信息、材料、计算、神经与自动化相关内容扩充；
- 完成材料科学证据包及后续 6+3 扩展；
- 补齐化学与化学工程、纳米科学、再生医学与组织工程、合成生物学等原空白门类；
- 第二批新增 ADC、长效 HIV 暴露前预防、纳米白蛋白结合药物、组织工程膀胱和 CAR-T 等代表性证据路径；
- 94 张证据卡均保存研究类型、适用对象、主要发现、限制、来源和最近核验日期。

### 3.2 分类结构收束

- `DOM-0002` 已并入 `DOM-0008`“自动化与控制科学”；
- “机器人学”已完整归入 `DOM-0001`“机械工程”，旧 `DOM-0003` 地址永久重定向到 `DOM-0001`；
- “化学”和“化学工程”已合并为基础科学门类 `DOM-0015`“化学与化学工程”，旧 `DOM-0016` 地址永久重定向到 `DOM-0015`；
- 自动化新增 `CAP-0037`“流程自动化与系统编排”，医学影像自动化和实验室自动化不再绕过核心能力层；
- 康复机器人由“闭环运动控制”承接；自动化门类当前没有直接连接技术的越层关系；
- 每个实体仍只有一个主导航归属，跨学科联系通过关系和交叉节点表达。

## 4. 审核与发布规则

项目采用 R1“来源核验型知识库”规则：独立医学专家审核是可选增强项，不是公开发布的前置条件。

公开内容必须满足：

1. 来源身份及可访问内容已经核对；
2. 研究对象、设计、终点、主要发现和局限均可由来源支持；
3. 监管批准、临床试验、早期人体研究、动物/体外研究和理论研究明确区分；
4. 阴性结果、安全性、利益冲突及外推边界不得被省略；
5. 数据校验、覆盖审计、类型检查和产品构建通过；
6. 页面明确显示非诊疗建议及专家审核状态。

94 张公开证据卡当前均为：

- `review_status: source-checked`；
- `expert_review_status: not-performed`；
- `publication_status: public`；
- 机器辅助整理，并明确披露未经独立医学专家审核。

未能完成逐条来源核验的内容应进入 `revise` 或 `blocked`，不能为了补齐页面而提升状态。被阻断的撤稿来源不得参与公开证据结论。

## 5. 页面与交互冻结状态

- 首页提供自然语言检索和可选阅读偏好，使用本地规则匹配，不进行疾病诊断；
- 首页已删除固定的“六类医学难题，六条技术探索路径”展示区，内容入口不再受固定数量限制；
- 首页云图以“医学×先进技术”为中心连接科学门类，并采用非对称、关系优先的人工布局；
- 首页云图支持拖动节点、空白处任意方向平移、触控板双指滑动平移、双指捏合缩放，以及独立的加、减和适应按钮；
- 点击或拖动节点时直接相关连线保持高亮；再次点击同一节点进入详情页；点击连线时仅高亮该线；高亮色与每条连线的箭头发出节点颜色一致，点击空白处取消高亮；
- 核心能力使用深绿，技术使用钴蓝，避免两类节点混淆；
- 完整图谱页已从拥挤的放射状云图改为按层级与类型分区的格子视图，详情按需展开；
- 实体、关系、证据和未来方向页面提供面包屑、返回路径和继续阅读入口；
- 实体页包含关系轨道、机制路径等数据驱动可视化；证据页包含 PICO、成熟度和时间线等结构化展示；
- “提交需求”前台及 Cloudflare Pages Functions + D1 接口代码已建立，但尚未接入真实云端数据库。

## 6. 技术基线与运行方式

当前技术栈保持为 Astro + React + Cytoscape.js + TypeScript，数据使用 JSON 与 Markdown 管理，尚未引入图数据库。

常用命令：

```bash
npm run dev
npm run validate:data
npm run audit:coverage
npm run check
npm run build
```

校验职责：

- `validate:data`：JSON Schema、引用完整性、状态约束和公开证据规则的硬校验；
- `audit:coverage`：批准门类覆盖、完整路径、关系重复、越层关系和证据集中度软审计；
- `check`：Astro 与 TypeScript 检查；
- `build`：完整静态站点构建。

已知环境问题（本地开发机）：

- `npm run check`（astro check）在当前开发机（Windows、Node 24）会因 Node 堆内存不足崩溃，报错为 `FATAL ERROR: Ineffective mark-compacts near heap limit - JavaScript heap out of memory`，退出码 134；通过 `NODE_OPTIONS="--max-old-space-size=8192"` 提高堆上限后仍然崩溃，系统物理内存充足（31.5 GB）；
- 该崩溃与代码改动无关：用 `git stash` 收起全部改动后在原始代码上运行，同样复现，属既有环境问题；
- 本地类型检查请改用 `npx tsc --noEmit`（已验证通过）；第 7 节中 `npm run check` 的验收记录为历史结果，不代表当前本机可复现；
- 该问题不影响 GitHub Actions：CI 在 ubuntu-latest 上运行 `npm run check` 与 `npm run build`，构建和部署正常。

部署路径约定（新增站内链接时必须遵守）：

- 站点部署在 GitHub Pages 子路径下，`astro.config.mjs` 配置了 `base: "/medtech-atlas/"`；
- Astro **不会**自动为 `.astro` 模板里的站内链接补 base 前缀，写 `href="/explore"` 会 404；
- `.astro` 模板统一使用 `src/lib/site.ts` 导出的 `sitePath("/explore")`；
- React 组件（`.tsx`）使用 `` `${import.meta.env.BASE_URL}entity/${id}` ``，注意拼接时不要加前导斜杠，否则会生成 `//` 双斜杠；
- 接收 href 数组的组件（`Breadcrumbs`、`ReturnPath`、`MechanismPath`、`ResearcherReading`）已在组件内部统一调用 `sitePath`，页面传参无需处理。

## 7. 本阶段验收结果

- `npm run validate:data`：通过；
- `npm run audit:coverage`：16/16 个批准门类落地，缺少 0 个，覆盖提醒 0 项；
- `npm run check`：36 个源文件，0 错误、0 警告、0 提示；
- `npm run build`：成功生成 461 个静态页面；
- 旧机器人学与旧化学工程地址均保留永久重定向；
- 导航不存在悬空引用、重复主归属或未归组实体；
- 当前分支为 `main`，远程仓库为 `github.com/i11umination/medtech-atlas`（仓库已于 2026-09-14 由 `i11umination.github.io` 重命名），站点发布在 `https://i11umination.github.io/medtech-atlas/`，由 GitHub Actions 自动部署到 Pages。

## 8. 尚未完成与已知风险

- 没有独立医学专家资源；当前所有证据卡均明确记录 `expert_review_status: not-performed`；
- EVD-0015 至 EVD-0055 仍适合在未来按风险分批进行更深的全文级复核，优先关注 EVD-0042、0044、0048—0049、0051、0054—0055 的利益冲突、安全事件和外推边界；
- 需求提交尚未部署，D1 数据库、Binding、管理审核入口和发布流水线没有接通；
- 自然语言检索仍为本地规则匹配，未使用浏览器端语义模型；
- 数据规模继续增长后，静态全量加载、检索性能和导航维护成本需要重新评估；
- 对外扩大传播前仍应补充隐私说明、内容更新机制，以及无障碍和多设备验收；
- `qa/` 和部分 `planning/` 文件保存历史快照，其中可能出现旧的 `needs-review` 或门类数量，仅用于追溯当时决策；当前状态以本交接、R1 规则和实时校验结果为准。

## 9. 下一阶段建议

下一阶段建议命名为：**既有门类第二路径扩展与证据维护**。

推荐顺序：

1. 对化学与化学工程、纳米科学、再生医学与组织工程、合成生物学补充第二条或第三条独立临床路径；
2. 优先新增能够降低卒中和运动康复证据占比的疾病门类与患者结局；
3. 按“门类 → 核心能力 → 技术 → 临床问题/疾病 → 证据”的完整路径入库，避免增加空节点；
4. 建立来源复核日期、撤稿/更正、阴性结果和利益冲突的定期维护清单；
5. 内容量和页面性能达到实际瓶颈后，再比较静态索引、轻量数据库和图数据库三套迁移方案；未经用户确认不更换技术栈。

## 10. 接手入口

建议按以下顺序阅读：

1. `HANDOFF.md`：当前冻结基线与下一阶段入口；
2. `planning/content-intake-process-v1.md`：R1 入库与发布流程；
3. `planning/content-ontology-v1.md`：五层内容模型、科学门类与导航规则；
4. `planning/second-batch-expansion-blueprint.md`：第二批选题、证据包与后续候选；
5. `qa/second-batch-expansion-validation.md`：本批内容和结构验收；
6. `qa/r1-review-policy-validation.md`：R1 状态模型与迁移记录；
7. `qa/frontend-prototype-validation.md`：当前页面和交互基线；
8. `claude.md`：完整历史决策记录；
9. `subagents.md`：批量内容工作的角色划分。

## 11. 存档说明

本阶段以 Git 提交、阶段标签和 ZIP 快照共同冻结：

- Git 标签：`stage-2-content-expansion-2026-09-13`；
- ZIP 文件：`archives/medical-advanced-technology-2026-09-13-stage-2-content-expansion.zip`；
- ZIP 由阶段提交直接生成，不包含 `node_modules/`、`dist/`、`.astro/` 或 `.git/`；
- 恢复或比较时优先使用 Git 标签，ZIP 作为离线副本。

至此，本阶段收尾完成。后续工作应从证据较薄门类的新增完整路径开始，而不是继续修改已经稳定的分类骨架。
