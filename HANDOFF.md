# 医学 × 先进技术项目交接

更新日期：2026-09-15

阶段状态：**A→B→C 阅读体验重构与实体通俗医学连接建设已完成；第三阶段 A 路线第一波已冻结，第二波七条路径已完成来源锁定、正式入库和本地验收，待用户决定是否创建冻结提交。**

项目定位：面向公众和研究者的中文医学技术知识导航站，通过可追溯来源解释科学技术如何进入医学问题；不提供诊断、治疗或个体化医疗建议。

本阶段归档标签：`archive/abc-reading-experience-20260914`

重构前对照点：提交 `0d393d7`，标签 `archive/pre-abc-refactor-20260914`

第三阶段第一波冻结提交：`0ec36e3`

第三阶段第一波冻结标签：`stage-3-wave-1-2026-09-15`

> 第一波冻结提交当前尚未推送远程；第二波尚未创建冻结提交或标签。对外推送或发布仍需用户明确确认。

## 1. 当前可用基线

| 对象 | 数量 | 状态 |
| --- | ---: | --- |
| 实体节点 | 198 | 数据校验通过 |
| 已批准科学门类 | 16 | 16/16 已落地，覆盖提醒 0 项 |
| 实体关系 | 242 | 数据校验通过 |
| 独立证据卡 | 132 | 全部为 `source-checked` |
| 通俗医学连接 | 198 | 每个实体一条，全部为 `source-checked` |
| 来源记录 | 194 | 含科普、机构、监管、指南和研究来源 |
| 未来方向 | 4 | 均绑定依据证据并标明为项目分析 |
| 构建产物 | 582 页 | 完整静态构建通过 |

第三阶段不针对脑卒中、肿瘤或其他疾病类型设置增减配额、比例目标或默认优先级；选题按证据可核验性、临床问题清晰度、图谱结构价值和内容可解释性评估。

## 2. 本阶段成果

### 2.1 首页与图谱交互

- 首页副标题更新为项目当前使命表述，内容入口不再受固定主题数量限制。
- 首页云图以“医学×先进技术”为中心，采用稳定、可重复的关系优先布局。
- 点击或拖动节点会高亮直接相关连线并显示上下文卡片；再次点击同一节点进入详情页。
- 点击连线会单独高亮该线并显示关系摘要；高亮色取自箭头发出节点，点击空白区域取消高亮。
- 云图支持节点拖动、画布平移、触控板双指平移与捏合缩放，以及独立的放大、缩小和适应按钮。
- 完整图谱页采用按层级和类型分区的格子视图，避免全量放射图在大型数据集下拥挤。
- 统一了研究阶段的公众表达；结构关系显示为“不按研究阶段划分”，不再出现意义不明的“不适用”。

### 2.2 实体页阅读体验

- 完成 A→B→C 阅读路径：先建立通俗理解，再查看关系位置，最后进入专业说明和证据。
- 新增读者角色阅读建议和吸顶页内导航，帮助公众、研究者等读者选择起点。
- 198 个实体页均默认显示分段、面向公众的“医学连接”，以理解为优先，不强行压缩篇幅。
- 科普解释依据与研究/专业依据分层展示；论文与系统综述不会冒充默认科普材料。
- “查看专业说明”入口已加强视觉权重；专业层只展示大众解释之外的术语、机制、证据阶段、限制和研究问题。
- 构建时按章节职责和文字重合度去重：94 个页面保留专业入口，79 个页面因没有新增信息而隐藏入口；共展示 209 个专业章节，省略 474 个重复或高重合章节，已展示章节最高重合度为 28.4%。
- 原有 Markdown 专业底稿完整保留，去重只影响页面展示，不破坏可追溯原文。
- 移除了详情页中没有后续阅读价值的“更新说明”等遗留展示。

### 2.3 关系浏览与响应式布局

- 实体页默认展示全部直接关联，并按证据数量、研究阶段和名称排序。
- “选择你想继续了解的内容类型”默认收起，展开后可以按科学门类、核心能力、技术、疾病等类型筛选。
- 关联卡片完成紧凑化：桌面三列、中等宽度两列、手机单列；筛选选项在桌面三列、手机两列。
- 关系轨道图在桌面提供空间位置概览，移动端改用直接相邻节点列表，避免横向拖动大型图形。
- 关系页、证据页和研究者阅读区统一了阶段标签、证据边界、面包屑、返回路径与继续阅读入口。

### 2.4 内容与审核体系

- `data/public-connections.json` 成为 198 个实体通俗医学连接及其科普来源映射的结构化数据源。
- JSON Schema、类型定义和数据校验脚本已覆盖通俗连接字段、来源引用、审核状态和日期。
- 新增专业层重复度审计，防止大众解释与专业说明再次演变为两份相同内容。
- 项目继续采用 R1“来源核验型知识库”规则。本项目不具备独立医学专家审校能力，不把专家审校列为发布关卡、增强路线或未来待办；页面仅保留“未经独立医学专家审核”的透明披露。
- 所有页面继续明确披露机器辅助整理、来源核验状态和非诊疗建议边界。

### 2.5 第三阶段第一波独立证据路径

- 化学与化学工程：新增“放射性配体设计与核素标记 → Pluvicto → PSMA 阳性 mCRPC 的靶向放射递送”路径，纳入 FDA 扩展适应证、VISION 和 PSMAfore。
- 纳米科学：新增“脂质体包封与肺部气溶胶递送 → ARIKAYCE → 难治性 MAC 肺病持续培养转阴”路径，明确有限人群、加速批准和临床获益未确立。
- 再生医学与组织工程：新增“脐带血造血祖细胞离体扩增 → Omisirge → 延迟植入与早期感染”路径，区分早期植入获益、未显著的生存/GVHD 结果和无对照长期随访。
- 合成生物学：新增“工程化溶瘤病毒 → T-VEC → 局部病灶控制与全身获益边界”路径，保留标签限制和联合 pembrolizumab 阴性 III 期结果。
- 本波净增 16 个实体、16 条关系、12 张证据卡、12 条来源及相应通俗连接和专业底稿；入库脚本为 `scripts/ingest-third-stage-wave-1.mjs`。

### 2.6 第三阶段第二波独立证据路径

- 自动化与控制科学：新增 Control-IQ 自动胰岛素递送路径，复用“实时反馈与自适应控制”，纳入成人/青少年和幼儿随机试验及带历史对照限制的 CLIO 真实世界研究。
- 免疫学：新增 blinatumomab 巩固治疗路径，分开成人 MRD 阴性缓解、儿童高危复发与 AALL1331 高中危复发证据，保留主要 DFS 未达预设阈值的阴性结果。
- 遗传学与基因组学：新增 HEMGENIX AAV5 体内基因治疗路径，使用 2026 年现行标签，并把 HOPE-B 18 个月、24 个月和 5 年结果标为同一 54 人单臂队列。
- 药理学与药剂学：新增 Wegovy/SELECT 心血管二级预防路径，同时呈现 MACE 绝对事件率、不良反应停药与同队列心衰预设分析。
- 物理学：新增 Paradise/Symplicity Spyral 导管肾动脉去神经路径；保留 SPYRAL ON MED 六个月主要比较未显著及 24 个月揭盲、交叉和缺失限制。
- 合成生物学：新增 SYNB1891 历史早期人体路径，明确 32 人 I 期不证明疗效且注册研究因商业原因终止。
- 再生医学与组织工程：新增 zimislecel 研究性胰岛细胞移植路径，复用既有细胞分化能力，明确 14 人非预设中期分析、免疫抑制、两例死亡、企业资助和未获批准状态。
- 本波净增 25 个实体、26 条关系、27 条来源、26 张证据卡及对应底稿；入库脚本为 `scripts/ingest-third-stage-wave-2.mjs`，验收记录为 `qa/third-stage-wave-2-validation.md`。
- 第一波四条 FDA 监管来源已同步复核并更新最近核验日期；未发现需要改变既有适应证或证据边界的情况。

## 3. 关键实现入口

| 路径 | 用途 |
| --- | --- |
| `data/public-connections.json` | 198 个实体的通俗医学连接与科普来源映射 |
| `data/sources.json` | 全部来源元数据及核验状态 |
| `src/pages/entity/[id].astro` | 实体页分层阅读主结构 |
| `src/components/EntityReadingGuide.tsx` | 读者角色与阅读起点建议 |
| `src/components/EntityVisualOverview.astro` | 当前节点的关系位置展示 |
| `src/components/RelatedContentExplorer.tsx` | 关系筛选、排序与卡片列表 |
| `src/components/MiniGraph.tsx` | 首页云图布局与节点/连线交互 |
| `src/lib/professionalSummary.ts` | 专业底稿解析、职责筛选与重合度去重 |
| `src/lib/researchStage.ts` | 研究阶段显示、提示与排序规则 |
| `scripts/audit-professional-overlap.mjs` | 198 页专业层去重审计 |
| `scripts/ingest-third-stage-wave-1.mjs` | 第三阶段第一波的可重复、幂等入库脚本 |
| `scripts/ingest-third-stage-wave-2.mjs` | 第三阶段第二波七包的可重复、幂等入库及第一波监管来源复核脚本 |
| `scripts/validate-data.mjs` | Schema、引用完整性和公开状态硬校验 |
| `src/styles/global.css` | 本阶段页面层级、卡片和响应式布局 |

## 4. 运行与验证

常用命令：

```bash
npm run dev
npm run validate:data
npm run audit:coverage
npm run audit:professional
npx tsc --noEmit
npm run build
```

如果本地首页云图空白且浏览器报 `504 Outdated Optimize Dep`，说明 Vite 的 Cytoscape 优化缓存过期，使用以下命令重启即可：

```bash
npm run dev -- --force --host 127.0.0.1
```

本阶段最终验收：

- `npm run validate:data`：通过；198 个节点、198 条通俗连接、242 条关系、132 张证据卡、194 条来源、4 条未来方向。
- `npm run audit:coverage`：通过；16/16 个批准门类已落地，覆盖提醒 0 项。
- `npm run audit:professional`：通过；198 页全部符合当前专业层去重阈值，119 页保留专业入口、79 页隐藏重复入口。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；生成 582 个静态页面。
- 首页云图和实体页关系卡片已在本地浏览器完成桌面、中等宽度和移动端检查。

`npm run check`（Astro Check）在当前 Windows + Node 24 环境中仍会因 Node 堆内存问题崩溃；原始版本同样可复现。当前本机以 `npx tsc --noEmit` 和完整构建作为类型与产物验收，GitHub Actions 仍会在 Ubuntu 上执行 `npm run check`。

## 5. 部署约定

- 远程仓库：`https://github.com/i11umination/medtech-atlas.git`
- GitHub Pages：`https://i11umination.github.io/medtech-atlas/`
- 部署分支：`main`；推送后由 `.github/workflows/deploy-pages.yml` 自动构建和部署。
- 站点使用 `base: "/medtech-atlas/"`。Astro 模板中的站内链接使用 `sitePath()`；React 组件使用 `import.meta.env.BASE_URL`，避免遗漏子路径或生成双斜杠。
- 对外发布、推送及修改 GitHub Pages 前仍需用户明确确认。

## 6. 尚未完成与风险

- 所有公开内容均完成来源级核对；项目不提供独立医学专家审校，不得把 `source-checked` 表述为临床专业背书。
- “提交需求”前台和 Cloudflare Pages Functions + D1 接口代码继续保留，但用户已决定当前阶段暂不接通真实数据库、Binding、审核入口和发布流程。
- 首页自然语言检索仍是浏览器端关键词、别名和规则匹配，不是语义模型，也不用于疾病诊断。
- 当前静态全量数据规模可用；如果节点、关系或证据显著增长，应先做真实加载与交互性能测量，再决定是否引入索引、轻量数据库或图数据库。
- 历史 `planning/`、`qa/` 和旧压缩包用于追溯，可能包含已经过时的数量、状态和设计方案；当前事实以本交接文档、实时数据校验和代码为准。

## 7. 后续开发入口

建议按以下顺序继续：

1. 先由用户决定是否为第二波创建本地冻结提交与标签；这不会自动推送或发布。
2. 若创建冻结点，再单独由用户决定是否推送远程并触发 GitHub Pages；不得把本地冻结授权视为发布授权。
3. 后续内容维护继续优先复核安全事件、监管补充、阴性结果、同队列重复和外推边界。
4. 新增内容坚持“门类 → 核心能力 → 技术 → 临床问题/疾病 → 证据”的可追溯路径，避免只增加孤立节点；不设置任何疾病比例目标。
5. 用户需求系统保持暂缓；未经用户重新确认，不配置 D1、Binding 或审核后台，也不因此更换当前技术栈。

接手时优先阅读：

1. `HANDOFF.md`：当前冻结状态和后续入口。
2. `README.md`：项目结构、运行方式和公开边界。
3. `planning/third-stage-wave-2-candidate-precheck.md`：第二波七条路径、来源候选和执行关卡。
4. `planning/content-intake-process-v1.md`：R1 内容入库与核验流程。
5. `planning/content-ontology-v1.md`：内容层级、科学门类和导航规则。

本阶段以 Git 提交和 `archive/abc-reading-experience-20260914` 标签冻结，不再额外生成重复的 ZIP 快照。
