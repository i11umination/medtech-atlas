# 项目交接摘要

交接日期：2026-09-12  
当前阶段：高质量静态原型、首批示范内容与大型内容库前置架构已完成  
项目状态：可继续作为内部原型评审；尚不适合作为已完成医学专家审核的公共知识库发布

## 1. 项目目标

建设一个中文医学与先进技术知识图谱，主要服务科研人员，同时为普通公众提供不失真的通俗解释。用户既可以从疾病、临床问题进入，也可以从科学门类、技术方向进入，最终查看研究证据、成熟度、限制和可能的未来研究方向。

网站主标语已经确定：

> 技术的终极目的，就是解决人类疾苦。

## 2. 当前已经完成

### 内容与数据

- 41 个实体节点；
- 48 条实体关系；
- 18 张独立证据卡；
- 32 条来源记录，其中 1 条撤稿来源已阻断；
- 4 条带依据证据的未来研究方向；
- 41 篇实体通俗简介；
- 10 个分层导航组，覆盖全部 41 个实体，主归属无重复、无遗漏；
- 8 个数据 JSON 文件均可解析并通过当前 JSON Schema。

首批疾病与临床主题包括脑卒中后运动功能障碍、脊髓损伤后运动功能障碍、膝/髋关节骨关节炎、截肢与肢体缺失后的功能重建、脑动脉瘤及相关神经血管疾病、脊柱退行性疾病。

### 页面与交互

- 首页自然语言输入框：用规则匹配把用户表达归纳到疾病、临床问题、技术或科学门类，不做诊断，也不依赖付费 AI；
- 可选用户身份和研究方向偏好：只影响推荐排序，不改变全局图谱；
- 分层图谱：全局框架、学科/疾病分组、主题子图和关系证据链逐层下钻；
- 主题子图最多展示焦点周围两跳、40 个节点，研究节点默认折叠；
- 实体、关系、证据和未来方向详情页均有面包屑及返回路径；
- 关联内容先选择五种卡片类型，再展开对应推荐；
- 实体页、关系页和证据页包含“研究者继续阅读”；
- 实体轨道图、五步机制路径、PICO、成熟度尺和研究时间线等数据驱动可视化；
- 首页右上角原创 SVG 徽标，以及同步视觉语言的页眉品牌图标和站点 favicon：锤子、试管分居上方两侧，扳手横放于医疗十字下方，三者形成三角结构并共同连接医学核心；
- “提交需求”入口和 Cloudflare Pages Functions + D1 接口代码已经建立，但尚未连接真实云端数据库。

### 当前验证结果

- `npm run check`：30 个源文件，0 错误、0 警告、0 提示；
- `npm run build`：成功生成 115 个静态页面；
- 首页和图谱探索页在本地开发服务器返回 HTTP 200；
- 导航分组没有悬空父子引用、实体引用或重复主归属。

## 3. 已确认、后续不得无故推翻的设计决策

- 第一版继续采用 Astro + React + Cytoscape.js + TypeScript；
- 内容使用分层 JSON 与 Markdown，暂不引入图数据库；
- 图谱是知识导航层，不直接铺开底层全部数据；
- 每个实体只有一个主导航归属，跨学科联系通过独立字段表达；
- 核心能力作为正式 `capability` 节点纳入图谱，使用 `CAP-*` 稳定 ID，不再只作为关系说明中的文字标签；
- 点击节点负责下钻，滚轮只缩放当前层；
- 科学门类采用“工程与信息科学、基础科学、生命与认知科学、交叉科学方向”四大组；
- “自动化”扩展为“自动化与控制科学”；
- 化学与化学工程前台可先共用入口，底层保留拆分能力；
- 新科学门类只有达到定义来源、代表技术、完整证据路径和审核门槛后，才生成正式实体页；
- AI 如果加入，只负责理解输入和匹配候选节点，不能生成未经来源支持的医学结论；优先保持免费、本地可运行；
- 用户提交只能进入待审核队列，不能自动写入公开医学知识库。

## 4. 尚未完成与主要风险

- 全部医学内容仍处于 `needs-review`，现有审核是来源与医学安全边界初审，不是执业医学专家签署；
- EVD-0015 至 EVD-0018 及其来源应优先进行全文级复核，重点检查异质性、预设亚组、安全事件、转换手工操作和外推边界；
- 需求提交功能尚未部署，D1 数据库、Binding、管理审核入口和发布流水线均未接通；
- 当前自然语言检索为本地规则匹配，尚未加入浏览器端语义模型；
- 分层图谱能控制单次展示规模，但大型内容库仍需要按需加载、后台检索和导航分组维护工具；
- 当前工作目录未被命令行识别为 Git 仓库。进入大规模内容扩展前，应由用户决定是否建立版本控制与备份流程；
- 公开发布前仍需补充隐私说明、内容更新机制、医学专家复核流程和无障碍/多设备浏览验收。

## 5. 下一阶段的明确任务

下一阶段命名为：**大型内容库第一批扩展与入库流程建设**。

### 阶段目标

在不降低医学可信度的前提下，将当前“手工示范数据”升级为可持续扩充的内容生产流程，并完成第一批新增科学门类的真实内容。

### 推荐执行顺序

1. **确定第一批内容蓝图**  
   为电子与信息工程、材料科学与工程、计算机科学与人工智能、神经科学与认知科学、自动化与控制科学分别列出核心能力、代表技术、优先医学问题和计划证据数量，提交用户审核。

2. **建立可重复的数据入库检查**  
   把当前 JSON Schema、ID 唯一性、关系端点、双向证据引用、导航归属、Markdown 存在性和高风险措辞检查整理成可重复运行的验证脚本。大规模增量内容必须先通过自动检查。

3. **按完整路径扩充，而不是堆积空节点**  
   每个新增门类先完成至少两条“科学门类 → 核心能力 → 技术 → 临床问题/疾病 → 研究证据 → 患者结局”路径，再发布实体页。

4. **完成第一批证据检索与提取**  
   第一批总体目标约 30—50 张高质量证据卡。优先连接现有脑卒中、脑动脉瘤/神经介入、康复机器人、智能假肢和脊柱手术主题；优先监管机构、临床试验注册、原始研究和系统综述。

5. **并行生成通俗解释与未来方向**  
   通俗内容必须保留研究对象、阶段、终点和局限；未来方向必须绑定依据证据，并明确标注为项目分析。

6. **医学安全审核后再进入公开图谱**  
   先做结构与来源审核，再做医学专家复核；未通过者保持 `needs-review`，不得因为页面需要而改成 `approved`。

7. **内容达到一定规模后再接后端**  
   根据实际数据量提出“静态索引、轻量数据库、图数据库”三套迁移方案供用户选择；在用户批准前不擅自更换技术栈。

### 下一次会话的第一项工作

直接从“第一批内容蓝图”开始：为上述五个科学门类各提出 2—4 条优先医学路径、预计节点与证据量、来源检索关键词和建设难度，形成可审核表格。用户批准后，再启动批量学术检索和数据录入。

## 6. 继续工作时优先阅读

1. `HANDOFF.md`：本交接摘要；
2. `claude.md`：完整产品决策和已审批方案；
3. `planning/content-ontology-v1.md`：大型内容库的学科分类、五层导航和发布门槛；
4. `subagents.md`：主控与七个专职 subagent 的职责和协作顺序；
5. `qa/medical-review-round-1.md`：医学初审结论和高优先级风险；
6. `qa/frontend-prototype-validation.md` 与 `qa/data-validation.md`：当前验证基线。

## 7. 交接结论

本阶段已经完成从产品构想、内容规范、首批真实数据到可浏览静态原型的闭环，并为大型内容库建立了分层导航和发布门槛。下一阶段不应继续零散添加页面功能，而应把工作重心转向第一批科学门类内容蓝图、自动化入库检查、批量证据建设和医学审核流程。
