# 医学 × 先进技术

> 技术的终极目的，就是解决人类疾苦。

这是一个面向科研人员、同时兼顾公众理解的中文医学技术知识导航站。项目从疾病和临床问题出发，追踪不同科学与技术路径正在解决什么、已经证明什么，以及距离真实患者获益还有多远。

本站用于科研信息检索和知识导航，不提供诊断、治疗建议或专业医学决策。

## 当前阶段

大型内容库第一、二批扩充及图谱结构收束已经完成，当前基线为：

| 数据对象 | 数量 |
| --- | ---: |
| 实体节点 | 157 |
| 科学门类 | 16 |
| 核心能力 | 37 |
| 技术 | 32 |
| 疾病与健康状态 | 25 |
| 临床问题 | 22 |
| 研究节点 | 25 |
| 实体关系 | 200 |
| 独立证据卡 | 94 |
| 来源记录 | 122 |
| 未来研究方向 | 4 |
| 导航分组 | 17 |

16 个批准科学门类已经全部落地，覆盖审计当前为 0 项提醒。脑卒中证据在疾病层占 22/94（23.4%），主要脑卒中临床问题占 20/94（21.3%）。

## 内容与分类

- “机器人学”已经完整归入“机械工程”，旧 `DOM-0003` 地址永久重定向到 `DOM-0001`；
- “化学”和“化学工程”已经合并为基础科学门类“化学与化学工程”，旧 `DOM-0016` 地址永久重定向到 `DOM-0015`；
- “自动化”已经扩展为“自动化与控制科学”，并新增“流程自动化与系统编排”核心能力；
- 医学影像自动化、实验室自动化和康复机器人均通过核心能力进入图谱，不再由科学门类直接越层连接技术；
- 每个实体只有一个主导航归属，跨学科联系通过关系和交叉节点表达；
- 新增内容按“科学门类 → 核心能力 → 技术 → 临床问题/疾病与健康状态 → 研究证据 → 患者结局”的完整路径组织。

## 页面与交互

- 首页提供自然语言检索和可选阅读偏好，使用本地规则进行候选匹配，不进行疾病诊断；
- 首页不再固定展示有限数量的医学主题，内容统一通过搜索和图谱进入；
- 首页云图以“医学×先进技术”为中心，采用非对称、关系优先的布局；
- 云图支持拖动节点、空白处自由平移、触控板双指滑动平移、双指捏合缩放，以及独立的加、减和适应按钮；
- 拖动节点时直接相关连线会以该节点的类型颜色高亮；
- 核心能力使用深绿，技术使用钴蓝，避免视觉混淆；
- 完整图谱页采用按层级和类型分区的格子视图，详情按需展开，不再绘制拥挤的全量放射状连线；
- 实体、关系、证据和未来方向页面均提供来源、限制、研究阶段和继续阅读入口。

## R1 来源核验规则

项目采用 R1“来源核验型知识库”规则。独立医学专家审核是可选增强项，不是公开发布的前置条件。

当前 94 张公开证据卡均为：

- `review_status: source-checked`；
- `expert_review_status: not-performed`；
- `publication_status: public`；
- 机器辅助整理，并明确披露未经独立医学专家审核。

公开内容必须经过逐条来源核对、事实边界检查、自动数据验证和产品构建验证。监管批准、临床试验、早期人体研究、动物/体外研究及理论研究必须明确区分；阴性结果、安全性、利益冲突和外推限制不得省略。

## 技术结构

- Astro：生成首页、实体页、关系页、证据页和方法页等静态内容；
- React：承载自然语言归纳、阅读偏好、首页云图和完整图谱格子浏览；
- Cytoscape.js：绘制首页及局部关系图；
- TypeScript：约束组件和知识图谱数据字段；
- JSON + Markdown：分别保存结构化数据、索引、通俗简介和证据原稿；
- Cloudflare Pages Functions + D1：预留用户内容需求收集接口，目前尚未连接真实云端数据库。

项目当前保持静态优先，不使用图数据库，也不依赖付费 AI 服务。

## 本地运行

```bash
npm install
npm run dev
```

默认预览地址为 `http://localhost:4321`。

## 验证

```bash
npm run validate:data
npm run audit:coverage
npm run check
npm run build
```

- `validate:data` 检查 JSON Schema、跨文件引用、Markdown 文件、导航归属、阻断来源和公开证据规则；
- `audit:coverage` 报告门类缺口、完整路径、重复关系、越层关系和证据集中度；
- `check` 执行 Astro 与 TypeScript 检查；
- `build` 生成完整静态站点至 `dist/`。

当前验证基线：157 个节点、200 条关系、94 张证据卡、122 条来源和 4 条未来方向通过数据校验；16/16 个批准门类落地，覆盖提醒为 0；36 个源文件无错误、警告或提示；构建生成 461 个静态页面。

## 需求收集部署准备

1. 复制 `wrangler.toml.example` 为 `wrangler.toml`，填入实际的 Cloudflare D1 `database_id`；
2. 使用 `migrations/0001_content_requests.sql` 创建 D1 表；
3. 将项目连接到 Cloudflare Pages，并配置 `DB` D1 binding；
4. 部署静态站点及 `functions/api/requests.ts`；
5. 用户需求进入 `submitted` 队列，经人工归类、来源核验和发布检查后，才可修改公开知识库。

本地 Astro 预览不会自动提供 Cloudflare D1 binding。未部署到 Pages 时，表单会明确提示数据库尚未连接，不会伪造提交成功。

## 文档与阶段存档

- [项目交接](HANDOFF.md)：当前冻结基线、风险和下一阶段入口；
- [R1 入库流程](planning/content-intake-process-v1.md)：内容发现、核验和发布规则；
- [内容本体](planning/content-ontology-v1.md)：五层模型、门类和导航原则；
- [第二批扩展蓝图](planning/second-batch-expansion-blueprint.md)：本批路径及后续候选；
- [第二批验收](qa/second-batch-expansion-validation.md)：内容、结构和验证结果。

当前阶段冻结于：

- Git 提交：`e18b3ce`；
- Git 标签：`stage-2-content-expansion-2026-09-13`；
- ZIP：`archives/medical-advanced-technology-2026-09-13-stage-2-content-expansion.zip`。

下一阶段建议从“既有门类第二路径扩展与证据维护”开始，优先补充化学与化学工程、纳米科学、再生医学与组织工程、合成生物学的独立临床路径。
